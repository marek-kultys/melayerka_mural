# Trowel & Lime — The Rare Art of Sgraffito

A Jekyll-based website for Trowel & Lime — Mela Yerka's mural business — showcasing traditional sgraffito mural art.

## Running locally

### Prerequisites

- Ruby (2.7 or higher recommended)
- Bundler (`gem install bundler`)

### Setup

1. Clone the repository:

2. Install dependencies:
   ```bash
   bundle install
   ```

3. Start the local server:
   ```bash
   bundle exec jekyll serve
   ```

4. Open your browser and visit: `http://localhost:4000`

### Live reload

To automatically reload the browser when files change:
```bash
bundle exec jekyll serve --livereload
```

---

### Potential redundancies:

.gallery.gallery
.gallery.info
